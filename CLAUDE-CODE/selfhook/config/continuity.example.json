{
  "_comment": "Copy to continuity.json. Set workspace to an existing absolute path. Every listed and capped file must exist within that workspace. Sections render in configuration order. A section without events renders for every event. Caps are enforced only when check_limits.py is wired into a local workflow. See !SPECS.md for the complete configuration contract.",
  "workspace": "<WORKSPACE>",
  "sections": [
    {
      "slug": "startup-files",
      "header": "CONFIGURED STARTUP FILES",
      "events": ["SessionStart", "PostCompact"],
      "text": "This session started or compacted. These files were not injected. Open them in the configured order. Higher-priority runtime or user instructions take precedence.",
      "read_files": {
        "dir": ".memory",
        "patterns": ["startup.md", "user-notes.md", "work-notes.md"]
      }
    }
  ],
  "caps": [
    { "path": ".memory/startup.md", "limit": 12000 },
    { "path": ".memory/user-notes.md", "limit": 12000 },
    { "path": ".memory/work-notes.md", "limit": 8000 }
  ]
}
