{
  "name": "selfhook",
  "slug": "selfhook",
  "version": "1.1.0",
  "substrate": "claude-code",
  "type": "hooks",
  "author": "Cael",
  "license": "Apache-2.0",
  "description": "Claude Code lifecycle package: SessionStart/PostCompact continuity renderer, PreCompact shallow .memory directory generator, and commit-time character-limit checker. All components use one validated JSON workspace configuration.",
  "entrypoints": {
    "hooks": [
      "src/selfhook.py",
      "src/identity_directory.py"
    ],
    "checker": "src/check_limits.py",
    "skill_optional": "SKILL.md"
  },
  "requires": {
    "runtime": "Claude Code with hooks support (SessionStart/PostCompact)",
    "python": ">=3.8"
  },
  "installs_to": {
    "config": "<PACKAGE_ROOT>/config/continuity.json (you, from the example)",
    "hook_registration": "<WORKSPACE>/.claude/settings.json (SessionStart, PostCompact, and PreCompact entries)",
    "checker_wiring": "your pre-commit hook or sync script"
  }
}
